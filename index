<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Palace Cars Agadir - Conduisez Votre Aventure</title>
    <!-- Load Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Configure Tailwind to use the 'Inter' font and a custom color palette -->
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Inter', 'sans-serif'],
                    },
                    colors: {
                        'primary-blue': '#0A74DA', // Couleur principale inspirée
                        'secondary-dark': '#1F2937', // Fond sombre pour contraste
                    }
                }
            }
        }
    </script>
    <style>
        /* Custom styles for the hero section background effect */
        .hero-bg {
            background: linear-gradient(135deg, #0A74DA 0%, #004d9a 100%);
        }
    </style>
</head>
<body class="font-sans bg-gray-50 text-secondary-dark">

    <!-- Header & Navigation -->
    <header class="sticky top-0 z-50 bg-white shadow-md">
        <div class="container mx-auto px-4 py-4 flex justify-between items-center">
            <!-- Logo -->
            <a href="#" class="text-2xl font-bold text-primary-blue hover:text-blue-700 transition duration-300">
                PALACE<span class="text-secondary-dark">CARS</span>
            </a>
            <!-- Desktop Navigation -->
            <nav class="hidden md:flex space-x-6">
                <a href="#" class="text-secondary-dark hover:text-primary-blue font-medium transition duration-300">Accueil</a>
                <a href="#" class="text-secondary-dark hover:text-primary-blue font-medium transition duration-300">Flotte</a>
                <a href="#" class="text-secondary-dark hover:text-primary-blue font-medium transition duration-300">Services</a>
                <a href="#" class="text-secondary-dark hover:text-primary-blue font-medium transition duration-300">FAQ</a>
                <a href="#" class="text-secondary-dark hover:text-primary-blue font-medium transition duration-300">Contact</a>
            </nav>
            <!-- Booking Button (Visible on all sizes) -->
            <button class="bg-primary-blue text-white py-2 px-4 rounded-lg shadow-lg hover:bg-blue-700 transition duration-300 transform hover:scale-105 hidden sm:block">
                Réserver Maintenant
            </button>
            <!-- Mobile Menu Icon (Placeholder for functionality) -->
            <button class="md:hidden text-secondary-dark p-2 rounded-lg hover:bg-gray-100 transition duration-300">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" />
                </svg>
            </button>
        </div>
    </header>

    <!-- Hero Section with Booking Form -->
    <main class="hero-bg py-16 md:py-24 text-white">
        <div class="container mx-auto px-4">
            <div class="max-w-3xl text-center mx-auto mb-12">
                <h1 class="text-4xl sm:text-5xl md:text-6xl font-extrabold mb-4 leading-tight">
                    Trouvez la Voiture Parfaite pour Votre Voyage à Agadir
                </h1>
                <p class="text-xl opacity-90">
                    Découvrez notre flotte diversifiée, offrant luxe, économie et 4x4 pour chaque aventure.
                </p>
            </div>

            <!-- Booking Form Card -->
            <div class="bg-white p-6 md:p-8 lg:p-10 rounded-xl shadow-2xl text-secondary-dark max-w-6xl mx-auto">
                <h2 class="text-2xl font-bold mb-6 text-center text-primary-blue">Réservation Rapide</h2>
                <form id="booking-form" class="space-y-4">
                    <!-- CHAMPS: Nom et Téléphone -->
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <div class="relative">
                            <label for="full-name" class="block text-sm font-medium text-gray-700 mb-1">Nom et Prénom</label>
                            <input type="text" id="full-name" name="full-name" placeholder="Votre Nom Complet" required
                                class="w-full p-3 border border-gray-300 rounded-lg focus:ring-primary-blue focus:border-primary-blue transition duration-300">
                        </div>
                        <div class="relative">
                            <label for="phone-number" class="block text-sm font-medium text-gray-700 mb-1">Numéro de Téléphone</label>
                            <input type="tel" id="phone-number" name="phone-number" placeholder="ex: 0659198122" required
                                class="w-full p-3 border border-gray-300 rounded-lg focus:ring-primary-blue focus:border-primary-blue transition duration-300">
                        </div>
                    </div>
                    
                    <!-- Location Inputs -->
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <div class="relative">
                            <label for="pickup-location" class="block text-sm font-medium text-gray-700 mb-1">Lieu de Prise en Charge</label>
                            <input type="text" id="pickup-location" name="pickup-location" placeholder="ex: Aéroport d'Agadir (AGA)" required
                                class="w-full p-3 border border-gray-300 rounded-lg focus:ring-primary-blue focus:border-primary-blue transition duration-300">
                        </div>
                        <div class="relative">
                            <label for="return-location" class="block text-sm font-medium text-gray-700 mb-1">Lieu de Retour</label>
                            <input type="text" id="return-location" name="return-location" placeholder="Identique à la prise en charge ou nouveau lieu" required
                                class="w-full p-3 border border-gray-300 rounded-lg focus:ring-primary-blue focus:border-primary-blue transition duration-300">
                        </div>
                    </div>

                    <!-- Date & Time Inputs -->
                    <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
                        <div>
                            <label for="pickup-date" class="block text-sm font-medium text-gray-700 mb-1">Date de Prise en Charge</label>
                            <input type="date" id="pickup-date" name="pickup-date" required
                                class="w-full p-3 border border-gray-300 rounded-lg focus:ring-primary-blue focus:border-primary-blue transition duration-300">
                        </div>
                        <div>
                            <label for="pickup-time" class="block text-sm font-medium text-gray-700 mb-1">Heure</label>
                            <input type="time" id="pickup-time" name="pickup-time" value="10:00" required
                                class="w-full p-3 border border-gray-300 rounded-lg focus:ring-primary-blue focus:border-primary-blue transition duration-300">
                        </div>
                        <div>
                            <label for="return-date" class="block text-sm font-medium text-gray-700 mb-1">Date de Retour</label>
                            <input type="date" id="return-date" name="return-date" required
                                class="w-full p-3 border border-gray-300 rounded-lg focus:ring-primary-blue focus:border-primary-blue transition duration-300">
                        </div>
                        <div>
                            <label for="return-time" class="block text-sm font-medium text-gray-700 mb-1">Heure</label>
                            <input type="time" id="return-time" name="return-time" value="10:00" required
                                class="w-full p-3 border border-gray-300 rounded-lg focus:ring-primary-blue focus:border-primary-blue transition duration-300">
                        </div>
                    </div>

                    <!-- Search Button -->
                    <button type="submit" id="submit-button"
                        class="w-full mt-6 bg-primary-blue text-white font-semibold py-3 rounded-lg shadow-xl hover:bg-blue-700 transition duration-300 transform hover:scale-[1.01] flex items-center justify-center space-x-2">
                        <span>Soumettre la Demande de Réservation</span>
                        <div id="loading-spinner" class="hidden h-5 w-5 border-4 border-t-white border-blue-200 rounded-full animate-spin"></div>
                    </button>
                </form>

                <!-- Message Box for Form Submission -->
                <div id="form-message" class="mt-4 p-3 rounded-lg hidden" role="alert"></div>
            </div>
        </div>
    </main>
    
    <!-- Featured Cars Section -->
    <section class="py-16 md:py-20 bg-white">
        <div class="container mx-auto px-4">
            <h2 class="text-3xl font-bold text-center mb-12 text-secondary-dark">Notre Flotte Populaire</h2>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                <!-- Car Card 1 -->
                <div class="bg-white border border-gray-200 rounded-xl shadow-lg overflow-hidden hover:shadow-xl transition duration-300">
                    <img src="https://placehold.co/600x400/0A74DA/FFFFFF?text=Voiture+Economique" alt="Voiture Économique" class="w-full h-48 object-cover">
                    <div class="p-6">
                        <h3 class="text-xl font-bold text-secondary-dark mb-2">Renault Clio ou Similaire</h3>
                        <p class="text-gray-600 mb-4">Compacte & Économe en Carburant</p>
                        <div class="flex justify-between items-center">
                            <span class="text-2xl font-extrabold text-primary-blue">
                                25€ <span class="text-base font-normal text-gray-500">/ jour</span>
                            </span>
                            <a href="#" class="bg-primary-blue text-white py-2 px-4 rounded-lg hover:bg-blue-700 transition duration-300">Voir Détails</a>
                        </div>
                    </div>
                </div>

                <!-- Car Card 2 -->
                <div class="bg-white border border-gray-200 rounded-xl shadow-lg overflow-hidden hover:shadow-xl transition duration-300">
                    <img src="https://placehold.co/600x400/1F2937/FFFFFF?text=4x4" alt="4x4" class="w-full h-48 object-cover">
                    <div class="p-6">
                        <h3 class="text-xl font-bold text-secondary-dark mb-2">Dacia Duster ou Similaire</h3>
                        <p class="text-gray-600 mb-4">Spacieuse & Prête pour l'Aventure</p>
                        <div class="flex justify-between items-center">
                            <span class="text-2xl font-extrabold text-primary-blue">
                                45€ <span class="text-base font-normal text-gray-500">/ jour</span>
                            </span>
                            <a href="#" class="bg-primary-blue text-white py-2 px-4 rounded-lg hover:bg-blue-700 transition duration-300">Voir Détails</a>
                        </div>
                    </div>
                </div>

                <!-- Car Card 3 -->
                <div class="bg-white border border-gray-200 rounded-xl shadow-lg overflow-hidden hover:shadow-xl transition duration-300">
                    <img src="https://placehold.co/600x400/0A74DA/FFFFFF?text=Voiture+de+Luxe" alt="Voiture de Luxe" class="w-full h-48 object-cover">
                    <div class="p-6">
                        <h3 class="text-xl font-bold text-secondary-dark mb-2">Mercedes Classe C ou Similaire</h3>
                        <p class="text-gray-600 mb-4">Confort & Style Premium</p>
                        <div class="flex justify-between items-center">
                            <span class="text-2xl font-extrabold text-primary-blue">
                                99€ <span class="text-base font-normal text-gray-500">/ jour</span>
                            </span>
                            <a href="#" class="bg-primary-blue text-white py-2 px-4 rounded-lg hover:bg-blue-700 transition duration-300">Voir Détails</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Why Choose Us / Services Section -->
    <section class="py-16 md:py-20 bg-secondary-dark text-white">
        <div class="container mx-auto px-4">
            <h2 class="text-3xl font-bold text-center mb-12">Pourquoi Choisir Palace Cars Agadir ?</h2>
            
            <div class="grid grid-cols-1 md:grid-cols-4 gap-8 text-center">
                <!-- Feature 1 -->
                <div class="p-6 rounded-xl border border-primary-blue hover:bg-gray-700 transition duration-300">
                    <div class="text-4xl mb-4 text-primary-blue">★</div>
                    <h3 class="text-xl font-semibold mb-2">Meilleur Prix Garanti</h3>
                    <p class="text-gray-300">Nous offrons les tarifs les plus compétitifs à Agadir sans frais cachés.</p>
                </div>
                <!-- Feature 2 -->
                <div class="p-6 rounded-xl border border-primary-blue hover:bg-gray-700 transition duration-300">
                    <div class="text-4xl mb-4 text-primary-blue">✅</div>
                    <h3 class="text-xl font-semibold mb-2">Assurance Complète Incluse</h3>
                    <p class="text-gray-300">Conduisez sans souci avec une couverture d'assurance complète sur tous les véhicules.</p>
                </div>
                <!-- Feature 3 -->
                <div class="p-6 rounded-xl border border-primary-blue hover:bg-gray-700 transition duration-300">
                    <div class="text-4xl mb-4 text-primary-blue">✈️</div>
                    <h3 class="text-xl font-semibold mb-2">Transfert Aéroport</h3>
                    <p class="text-gray-300">Livraison et récupération gratuites à l'Aéroport d'Agadir Al Massira (AGA).</p>
                </div>
                <!-- Feature 4 -->
                <div class="p-6 rounded-xl border border-primary-blue hover:bg-gray-700 transition duration-300">
                    <div class="text-4xl mb-4 text-primary-blue">📞</div>
                    <h3 class="text-xl font-semibold mb-2">Assistance Routière 24/7</h3>
                    <p class="text-gray-300">Notre équipe de support est toujours prête à vous assister, jour et nuit.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white py-10">
        <div class="container mx-auto px-4 grid grid-cols-1 md:grid-cols-4 gap-8">
            <div>
                <h4 class="text-xl font-bold mb-4 text-primary-blue">Palace Cars</h4>
                <p class="text-gray-400">Votre partenaire de confiance pour la location de voiture à Agadir. Explorez le Maroc avec liberté et confort.</p>
            </div>
            <div>
                <h4 class="text-lg font-semibold mb-4 border-b border-primary-blue pb-2">Liens Rapides</h4>
                <ul class="space-y-2">
                    <li><a href="#" class="text-gray-400 hover:text-white transition duration-300">Liste de la Flotte</a></li>
                    <li><a href="#" class="text-gray-400 hover:text-white transition duration-300">Tarifs</a></li>
                    <li><a href="#" class="text-gray-400 hover:text-white transition duration-300">Termes & Conditions</a></li>
                </ul>
            </div>
            <div>
                <h4 class="text-lg font-semibold mb-4 border-b border-primary-blue pb-2">Infos Contact</h4>
                <p class="text-gray-400">Email: info@palacecars.com</p>
                <p class="text-gray-400">Téléphone: +212 659198122</p>
                <p class="text-gray-400">Adresse: Agadir, Maroc</p>
            </div>
            <div>
                <h4 class="text-lg font-semibold mb-4 border-b border-primary-blue pb-2">Suivez-nous</h4>
                <div class="flex space-x-4">
                    <!-- Social Media Links (Placeholders) -->
                    <a href="#" class="text-gray-400 hover:text-primary-blue transition duration-300 text-2xl">F</a>
                    <a href="#" class="text-gray-400 hover:text-primary-blue transition duration-300 text-2xl">I</a>
                    <a href="#" class="text-gray-400 hover:text-primary-blue transition duration-300 text-2xl">W</a>
                </div>
            </div>
        </div>
        <div class="mt-8 pt-4 border-t border-gray-700 text-center text-gray-500">
            &copy; <span id="current-year"></span> Palace Cars Agadir. Tous Droits Réservés.
        </div>
    </footer>

    <!-- Firebase Imports & Logic -->
    <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-app.js";
        import { getAuth, signInAnonymously, signInWithCustomToken } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-auth.js";
        import { getFirestore, collection, addDoc, setLogLevel } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-firestore.js";

        // IMPORTANT: Use the global variables provided by the Canvas environment
        const appId = typeof __app_id !== 'undefined' ? __app_id : 'default-app-id';
        const firebaseConfig = typeof __firebase_config !== 'undefined' ? JSON.parse(__firebase_config) : null;
        const initialAuthToken = typeof __initial_auth_token !== 'undefined' ? __initial_auth_token : null;

        let db, auth;
        const WHATSAPP_NUMBER = '212659198122';
        const BOOKINGS_COLLECTION_PATH = `artifacts/${appId}/public/data/bookings`;

        // Function to initialize Firebase
        async function initializeFirebase() {
            if (!firebaseConfig) {
                console.error("Firebase config is missing.");
                return;
            }

            try {
                // setLogLevel('debug'); // Uncomment to see Firebase logs
                const app = initializeApp(firebaseConfig);
                db = getFirestore(app);
                auth = getAuth(app);

                // Authenticate using custom token or anonymously
                if (initialAuthToken) {
                    await signInWithCustomToken(auth, initialAuthToken);
                } else {
                    await signInAnonymously(auth);
                }
                console.log("Firebase et Firestore initialisés.");
                
            } catch (error) {
                console.error("Erreur lors de l'initialisation de Firebase:", error);
                const messageBox = document.getElementById('form-message');
                messageBox.className = 'mt-4 p-3 bg-red-100 text-red-700 rounded-lg';
                messageBox.innerHTML = `<p class="font-bold">Erreur de service:</p><p>Le système de réservation est temporairement indisponible. Veuillez nous appeler directement au +${WHATSAPP_NUMBER}.</p>`;
                messageBox.classList.remove('hidden');
            }
        }

        // --- Booking Submission Logic ---
        document.addEventListener('DOMContentLoaded', () => {
            document.getElementById('current-year').textContent = new Date().getFullYear();
            
            initializeFirebase();

            const form = document.getElementById('booking-form');
            const messageBox = document.getElementById('form-message');
            const submitButton = document.getElementById('submit-button');
            const loadingSpinner = document.getElementById('loading-spinner');

            form.addEventListener('submit', async function(e) {
                e.preventDefault();

                if (!form.checkValidity()) { return; }

                if (!db || !auth || !auth.currentUser) {
                    messageBox.className = 'mt-4 p-3 bg-red-100 text-red-700 rounded-lg';
                    messageBox.innerHTML = '<p class="font-bold">Erreur de service:</p><p>Le service de base de données n\'est pas prêt. Veuillez réessayer ou nous contacter au +212 659198122.</p>';
                    messageBox.classList.remove('hidden');
                    return;
                }

                // Afficher le spinner et désactiver le bouton
                submitButton.disabled = true;
                loadingSpinner.classList.remove('hidden');
                submitButton.querySelector('span').textContent = 'Envoi...';
                messageBox.classList.add('hidden'); // Cacher les messages précédents
                
                try {
                    // 1. Collecte des données du formulaire
                    const bookingData = {
                        clientName: document.getElementById('full-name').value.trim(),
                        clientPhone: document.getElementById('phone-number').value.trim(),
                        pickupLocation: document.getElementById('pickup-location').value.trim(),
                        returnLocation: document.getElementById('return-location').value.trim(),
                        pickupDate: document.getElementById('pickup-date').value,
                        pickupTime: document.getElementById('pickup-time').value,
                        returnDate: document.getElementById('return-date').value,
                        returnTime: document.getElementById('return-time').value,
                        reservationDate: new Date().toISOString(),
                        status: 'Pending'
                    };

                    // 2. Enregistrement dans Firestore (silent "bot" action)
                    const bookingsCollection = collection(db, BOOKINGS_COLLECTION_PATH);
                    await addDoc(bookingsCollection, bookingData);

                    console.log("Réservation enregistrée silencieusement dans Firestore.");

                    // 3. Affichage du message de confirmation pour le client
                    messageBox.className = 'mt-4 p-3 bg-green-100 text-green-700 rounded-lg';
                    messageBox.innerHTML = `
                        <p class="font-bold">Demande Envoyée avec Succès !</p>
                        <p>Votre demande de réservation a été enregistrée. Nous allons vous appeler au ${bookingData.clientPhone} dans les plus brefs délais pour confirmer les détails et la disponibilité.</p>
                    `;
                    messageBox.classList.remove('hidden');

                    form.reset();

                } catch (error) {
                    console.error("Erreur lors de l'envoi de la réservation:", error);
                    messageBox.className = 'mt-4 p-3 bg-red-100 text-red-700 rounded-lg';
                    messageBox.innerHTML = `
                        <p class="font-bold">Erreur de Réservation :</p>
                        <p>Une erreur s'est produite. Veuillez réessayer ou nous contacter directement au +212 659198122.</p>
                    `;
                    messageBox.classList.remove('hidden');
                } finally {
                    // Masquer le spinner et réactiver le bouton
                    loadingSpinner.classList.add('hidden');
                    submitButton.querySelector('span').textContent = 'Soumettre la Demande de Réservation';
                    submitButton.disabled = false;
                }
            });
        });
    </script>
</body>
</html>
